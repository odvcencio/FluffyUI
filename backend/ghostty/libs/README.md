# libghostty binaries

Place prebuilt libghostty binaries under OS/arch subdirectories:

- `linux_amd64/libghostty.so`
- `darwin_arm64/libghostty.dylib`
- `windows_amd64/ghostty.dll`

Override the lookup path with `FLUFFYUI_GHOSTTY_LIB`.
