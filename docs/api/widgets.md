# Widgets API (Generated)

> This file is generated by `go run ./tools/gen_widgets_api`. Do not edit by hand.

FluffyUI widgets are constructed via `New*` functions and configured with option helpers.

## Widget Reference

### Accordion

Accordion groups collapsible sections.

Constructors:
- `NewAccordion(sections ...*AccordionSection) *Accordion`

Example:

```go
accordion := widgets.NewAccordion()
```

### AccordionSection

AccordionSection is a single collapsible section.

Constructors:
- `NewAccordionSection(title string, content runtime.Widget, opts ...AccordionSectionOption) *AccordionSection`

Example:

```go
accordionSection := widgets.NewAccordionSection("", nil)
```

### Alert

Alert renders an inline message.

Constructors:
- `NewAlert(text string, variant AlertVariant) *Alert`

Example:

```go
alert := widgets.NewAlert("", AlertVariant(""))
```

### AnimatedGauge

AnimatedGauge renders a radial gauge with spring animation.

Constructors:
- `NewAnimatedGauge(minValue, maxValue float64) *AnimatedGauge`

Example:

```go
animatedGauge := widgets.NewAnimatedGauge(0, 0)
```

### AnimatedWidget

AnimatedWidget is a base for widgets with animation support.

Constructors:
- `NewAnimatedWidget() AnimatedWidget`

Example:

```go
animatedWidget := widgets.NewAnimatedWidget()
```

### AspectRatio

AspectRatio constrains a child to a fixed width/height ratio.

Constructors:
- `NewAspectRatio(child runtime.Widget, ratio float64) *AspectRatio`

Example:

```go
aspectRatio := widgets.NewAspectRatio(nil, 0)
```

### AsyncImage

AsyncImage loads an image asynchronously and renders it when ready.

Constructors:
- `NewAsyncImage(path string, opts ...AsyncImageOption) *AsyncImage`
- `NewAsyncImageWithLoader(loader func() (image.Image, error), opts ...AsyncImageOption) *AsyncImage`

Example:

```go
asyncImage := widgets.NewAsyncImage("")
```

### AutoComplete

AutoComplete provides an input with suggestion list.

Constructors:
- `NewAutoComplete() *AutoComplete`

Example:

```go
autoComplete := widgets.NewAutoComplete()
```

### BarChart

BarChart renders horizontal bars.

Constructors:
- `NewBarChart(data *state.Signal[[]BarData]) *BarChart`

Example:

```go
barChart := widgets.NewBarChart(nil)
```

### Box

Box is a simple container that fills its background.

Constructors:
- `NewBox(child runtime.Widget, opts ...BoxOption) *Box`

Example:

```go
box := widgets.NewBox(nil)
```

### Breadcrumb

Breadcrumb renders a path of items.

Constructors:
- `NewBreadcrumb(items ...BreadcrumbItem) *Breadcrumb`

Example:

```go
breadcrumb := widgets.NewBreadcrumb()
```

### Button

Button is a clickable action widget.

Constructors:
- `NewButton(label string, opts ...ButtonOption) *Button`

Example:

```go
button := widgets.NewButton("")
```

### Calendar

Calendar displays a month grid with selectable days.

Constructors:
- `NewCalendar(opts ...CalendarOption) *Calendar`

Example:

```go
calendar := widgets.NewCalendar()
```

### CanvasWidget

CanvasWidget is a widget that draws using a Canvas.

Constructors:
- `NewCanvasWidget(draw func(canvas *graphics.Canvas), opts ...CanvasOption) *CanvasWidget`

Example:

```go
canvasWidget := widgets.NewCanvasWidget(nil)
```

### Checkbox

Checkbox is a toggle input widget.

Constructors:
- `NewCheckbox(label string) *Checkbox`

Example:

```go
checkbox := widgets.NewCheckbox("")
```

### DataGrid

DataGrid is a table with per-cell selection and inline editing.

Constructors:
- `NewDataGrid(columns ...TableColumn) *DataGrid`

Example:

```go
dataGrid := widgets.NewDataGrid()
```

### DatePicker

DatePicker combines a text input and calendar.

Constructors:
- `NewDatePicker() *DatePicker`

Example:

```go
datePicker := widgets.NewDatePicker()
```

### DateRangePicker

DateRangePicker combines two inputs with a range-select calendar.

Constructors:
- `NewDateRangePicker() *DateRangePicker`

Example:

```go
dateRangePicker := widgets.NewDateRangePicker()
```

### DebugOverlay

DebugOverlay draws widget bounds and labels for layout debugging.

Constructors:
- `NewDebugOverlay(root runtime.Widget, opts ...DebugOverlayOption) *DebugOverlay`

Example:

```go
debugOverlay := widgets.NewDebugOverlay(nil)
```

### Dialog

Dialog is a modal message container with optional custom content,

Constructors:
- `NewDialog(title, body string, buttons ...DialogButton) *Dialog`

Example:

```go
dialog := widgets.NewDialog("", "")
```

### EnhancedPalette

EnhancedPalette wraps a command registry with palette UI.

Constructors:
- `NewEnhancedPalette(registry *keybind.CommandRegistry) *EnhancedPalette`

Example:

```go
enhancedPalette := widgets.NewEnhancedPalette(nil)
```

### GPUCanvasWidget

GPUCanvasWidget draws using a GPU canvas and image protocols.

Constructors:
- `NewGPUCanvasWidget(draw func(canvas *gpu.GPUCanvas), opts ...GPUCanvasOption) *GPUCanvasWidget`

Example:

```go
gpuCanvasWidget := widgets.NewGPUCanvasWidget(nil)
```

### Grid

Grid lays out children in rows and columns.

Constructors:
- `NewGrid(rows, cols int) *Grid`

Example:

```go
grid := widgets.NewGrid(0, 0)
```

### Input

Input is a text input widget with cursor support.

Constructors:
- `NewInput() *Input`

Example:

```go
input := widgets.NewInput()
```

### Label

Label is a single-line text widget often used for headers/labels.

Constructors:
- `NewLabel(text string, opts ...LabelOption) *Label`

Example:

```go
label := widgets.NewLabel("")
```

### LineChart

LineChart renders one or more series using a CanvasWidget.

Constructors:
- `NewLineChart() *LineChart`

Example:

```go
lineChart := widgets.NewLineChart()
```

### Menu

Menu renders a vertical menu.

Constructors:
- `NewMenu(items ...*MenuItem) *Menu`

Example:

```go
menu := widgets.NewMenu()
```

### MultiSelect

MultiSelect renders a list of options with multiple selection.

Constructors:
- `NewMultiSelect(options ...MultiSelectOption) *MultiSelect`

Example:

```go
multiSelect := widgets.NewMultiSelect()
```

### MultilineInput

MultilineInput is a text input that supports multiple lines.

Constructors:
- `NewMultilineInput() *MultilineInput`

Example:

```go
multilineInput := widgets.NewMultilineInput()
```

### PaletteWidget

PaletteWidget provides a fuzzy-filtering command palette overlay.

Constructors:
- `NewPaletteWidget(title string) *PaletteWidget`

Example:

```go
paletteWidget := widgets.NewPaletteWidget("")
```

### Panel

Panel is a container widget with optional border and background.

Constructors:
- `NewPanel(child runtime.Widget, opts ...PanelOption) *Panel`

Example:

```go
panel := widgets.NewPanel(nil)
```

### PerformanceDashboard

PerformanceDashboard renders render-loop performance metrics.

Constructors:
- `NewPerformanceDashboard(sampler *runtime.RenderSampler, opts ...PerformanceDashboardOption) *PerformanceDashboard`

Example:

```go
performanceDashboard := widgets.NewPerformanceDashboard(nil)
```

### Popover

Popover positions a child widget relative to an anchor rect.

Constructors:
- `NewPopover(anchor runtime.Rect, child runtime.Widget, opts ...PopoverOption) *Popover`

Example:

```go
popover := widgets.NewPopover(runtime.Rect{}, nil)
```

### Progress

Progress displays a determinate progress bar.

Constructors:
- `NewProgress() *Progress`

Example:

```go
progress := widgets.NewProgress()
```

### Radio

Radio is a single radio option.

Constructors:
- `NewRadio(label string, group *RadioGroup) *Radio`

Example:

```go
radio := widgets.NewRadio("", nil)
```

### RadioGroup

RadioGroup manages a set of radio buttons.

Constructors:
- `NewRadioGroup() *RadioGroup`

Example:

```go
radioGroup := widgets.NewRadioGroup()
```

### RangeSlider

RangeSlider is a dual-handle slider.

Constructors:
- `NewRangeSlider(minValue, maxValue *state.Signal[float64], opts ...RangeSliderOption) *RangeSlider`

Example:

```go
rangeSlider := widgets.NewRangeSlider(nil, nil)
```

### RichText

RichText renders markdown content with scrolling.

Constructors:
- `NewRichText(content string, opts ...RichTextOption) *RichText`

Example:

```go
richText := widgets.NewRichText("")
```

### ScrollView

ScrollView provides a scrollable container.

Constructors:
- `NewScrollView(content runtime.Widget) *ScrollView`

Example:

```go
scrollView := widgets.NewScrollView(nil)
```

### SearchWidget

SearchWidget provides a search input overlay for the chat view.

Constructors:
- `NewSearchWidget() *SearchWidget`

Example:

```go
searchWidget := widgets.NewSearchWidget()
```

### Section

Section represents a collapsible section in the sidebar.

Constructors:
- `NewSection(title string) *Section`

Example:

```go
section := widgets.NewSection("")
```

### Select

Select is a dropdown-like selector (inline).

Constructors:
- `NewSelect(options ...SelectOption) *Select`

Example:

```go
select := widgets.NewSelect()
```

### SignalLabel

SignalLabel is a tiny label bound to a signal.

Constructors:
- `NewSignalLabel(source state.Readable[string], scheduler state.Scheduler) *SignalLabel`

Example:

```go
signalLabel := widgets.NewSignalLabel(nil, nil)
```

### SimpleWidget

SimpleWidget provides function hooks for quick widgets with Base styling.

Constructors:
- `NewSimpleWidget() *SimpleWidget`

Example:

```go
simpleWidget := widgets.NewSimpleWidget()
```

### Slider

Slider is a focusable value slider.

Constructors:
- `NewSlider(value *state.Signal[float64], opts ...SliderOption) *Slider`

Example:

```go
slider := widgets.NewSlider(nil)
```

### Sparkline

Sparkline renders a compact single-line chart.

Constructors:
- `NewSparkline(data *state.Signal[[]float64]) *Sparkline`

Example:

```go
sparkline := widgets.NewSparkline(nil)
```

### Spinner

Spinner is an animated loading indicator.

Constructors:
- `NewSpinner() *Spinner`

Example:

```go
spinner := widgets.NewSpinner()
```

### Splitter

Splitter divides space between two panes.

Constructors:
- `NewSplitter(first, second runtime.Widget) *Splitter`

Example:

```go
splitter := widgets.NewSplitter(nil, nil)
```

### Stack

Stack overlays child widgets.

Constructors:
- `NewStack(children ...runtime.Widget) *Stack`

Example:

```go
stack := widgets.NewStack()
```

### Stepper

Stepper renders a sequence of steps.

Constructors:
- `NewStepper(steps ...Step) *Stepper`

Example:

```go
stepper := widgets.NewStepper()
```

### Table

Table is a simple data grid widget.

Constructors:
- `NewTable(columns ...TableColumn) *Table`

Example:

```go
table := widgets.NewTable()
```

### Tabs

Tabs is a tabbed container widget.

Constructors:
- `NewTabs(tabs ...Tab) *Tabs`

Example:

```go
tabs := widgets.NewTabs()
```

### Text

Text is a simple text display widget.

Constructors:
- `NewText(text string, opts ...TextOption) *Text`

Example:

```go
text := widgets.NewText("")
```

### TextArea

TextArea is a multi-line text input widget.

Constructors:
- `NewTextArea() *TextArea`

Example:

```go
textArea := widgets.NewTextArea()
```

### TimePicker

TimePicker allows selecting a time of day.

Constructors:
- `NewTimePicker() *TimePicker`

Example:

```go
timePicker := widgets.NewTimePicker()
```

### ToastStack

ToastStack renders toast notifications.

Constructors:
- `NewToastStack() *ToastStack`

Example:

```go
toastStack := widgets.NewToastStack()
```

### Tooltip

Tooltip displays content anchored to a target widget.

Constructors:
- `NewTooltip(target runtime.Widget, content runtime.Widget, opts ...TooltipOption) *Tooltip`

Example:

```go
tooltip := widgets.NewTooltip(nil, nil)
```

### Tree

Tree renders a hierarchical tree.

Constructors:
- `NewTree(root *TreeNode) *Tree`

Example:

```go
tree := widgets.NewTree(nil)
```

### VideoPlayer

VideoPlayer renders video frames onto a canvas.

Constructors:
- `NewVideoPlayer(path string, opts ...VideoPlayerOption) (*VideoPlayer, error)`

Example:

```go
videoPlayer, err := widgets.NewVideoPlayer("")
if err != nil {
	// handle error
}
```

