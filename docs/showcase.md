# Widget Showcase

FluffyUI includes a curated showcase demo that highlights core widgets in a
single app. Run it locally:

```bash
go run ./examples/showcase
```

The docs TUI viewer (`go run ./docs/site`) will pick up this page as part of the
site navigation.
